server:
  port: 91

spring:
  application:
    name: goop-member

  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.56.10:3306/db_ums
    driver-class-name: com.mysql.jdbc.Driver

  cloud:
    nacos:
      discovery:
        server-addr: 192.168.56.10:8848

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

